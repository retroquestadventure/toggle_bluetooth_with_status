- Works now with Regestry
 - Key would be created if Key is not available
   The Key is in :
   Computer\HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\bthserv\Parameters
   -> Key: RadioSupport Value: 1 ( if ON ) or 0 ( if OFF )
   

- Sometimes the Tab Keys works not correct if the settings menü os already open. We go save and close it at first, if it is already open
